# AegisNode Code Formatting Rules (rustfmt)
# Định dạng code thống nhất trên toàn bộ workspace monorepo (Tương thích Stable Rust)

edition = "2024"
max_width = 100
tab_spaces = 4
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
